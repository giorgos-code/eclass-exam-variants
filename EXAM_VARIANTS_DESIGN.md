# Σχεδιασμός: Export Τεστ σε PDF με Ισοδύναμες Ομάδες

**Module:** `modules/exercise/`
**Σκοπός:** Ο καθηγητής επιλέγει ερωτήσεις (π.χ. 20), ορίζει Ν ομάδες και Μ ερωτήσεις ανά ομάδα, και λαμβάνει ένα PDF με τα Ν διαφορετικά τεστάκια έτοιμα για εκτύπωση.

**Καμία αλλαγή στη βάση δεδομένων.** Λειτουργεί εξ ολοκλήρου in-memory, πάνω στα υπάρχοντα δεδομένα.

---

## 1. Τι υπάρχει ήδη και θα αξιοποιηθεί

| Αρχείο | Τι παρέχει |
|---|---|
| `modules/exercise/export.php` | mPDF instance, CSS για print, `question_html()` function που αποδίδει μια ερώτηση με τις απαντήσεις της |
| `include/pdf_view.php` | `pdf_view()` helper με mPDF (με Greek fonts) |
| `modules/exercise/question_pool.php` | UI φίλτρων (κατηγορία, δυσκολία) και DataTable ερωτήσεων |
| `modules/exercise/question.class.php` | `Question::read()`, `selectTitle()`, `selectType()` κ.λπ. |
| `modules/exercise/answer.class.php` | `Answer` για ανάκτηση απαντήσεων κάθε ερώτησης |
| `include/constants.php` | UNIQUE_ANSWER, MULTIPLE_ANSWER, TRUE_FALSE, κ.λπ. |

---

## 2. Νέα αρχεία που θα δημιουργηθούν

### 2.1. `modules/exercise/pdf_exam_groups.php` (ΝΕΟ)

Κεντρικό entry point. Χειρίζεται δύο φάσεις:

- **GET** → εμφανίζει τη φόρμα επιλογής ερωτήσεων & παραμέτρων.
- **POST** → τρέχει τον αλγόριθμο και παράγει PDF μέσω mPDF.

**Γιατί ένα αρχείο και όχι δύο (φόρμα + generator):** Η λογική είναι απλή (< 300 γραμμές) και τα δεδομένα ρέουν ευθύγραμμα: φόρμα → POST → PDF. Ο κώδικας του `export.php` ακολουθεί το ίδιο μοτίβο (όλα σε ένα αρχείο).

---

### 2.2. Φόρμα επιλογής (Βήμα 1 — GET)

Η σελίδα θα έχει τρία τμήματα:

#### Τμήμα Α: Πηγή ερωτήσεων

Ο καθηγητής επιλέγει από πού θα τραβήξει τις ερωτήσεις:

```
● Από συγκεκριμένο exercise:  [dropdown με τα exercises του μαθήματος]
○ Από το question pool        Κατηγορία: [dropdown]  Δυσκολία: [dropdown]
```

**Γιατί και τα δύο;** Το συνηθέστερο use case: ο καθηγητής έχει ήδη ετοιμάσει ένα exercise με 20 ερωτήσεις και θέλει 3 PDFs. Αλλά μπορεί επίσης να θέλει να επιλέξει από το γενικό pool χωρίς να έχει exercise.

#### Τμήμα Β: Επιλογή ερωτήσεων

Λίστα με checkboxes — παρόμοια με το `question_pool.php`. Κάθε γραμμή δείχνει:
- Checkbox
- ID
- Κείμενο ερώτησης (truncated)
- Τύπος
- Κατηγορία
- Δυσκολία
- Βαθμός (weighting)

Κουμπιά: «Επέλεξε Όλες» / «Καμία».

#### Τμήμα Γ: Παράμετροι παραγωγής

```
Τίτλος εξέτασης:   [___________________________]   (π.χ. "Διαγώνισμα Β' Τετραμήνου")
Μάθημα:            [___________________________]   (pre-filled: currentCourseName)
Ημερομηνία:        [  ημ/μ/έτος  ]
Διάρκεια (λεπτά):  [ 45 ]

Αριθμός ομάδων:    [ 2 ]   (Ν, 1–26)
Ερωτήσεις/ομάδα:  [ 10 ]  (Μ)

Ονόματα ομάδων:    ● Αυτόματα (Α, Β, Γ…)
                   ○ Χειροκίνητα: [Ομάδα 1] [Ομάδα 2] …

□ Εμφάνιση σωστών απαντήσεων (answer key) στο τέλος του PDF
□ Ανακάτεμα σειράς επιλογών (shuffled options) εντός κάθε ερώτησης
```

---

## 3. Αλγόριθμος παραγωγής (Βήμα 2 — POST)

```
Είσοδος:
  question_ids[]  = επιλεγμένες ερωτήσεις (τουλάχιστον M)
  N               = αριθμός ομάδων
  M               = ερωτήσεις ανά ομάδα
  shuffle_options = bool

Έλεγχος: αν count(question_ids) < M → σφάλμα "Επέλεξε τουλάχιστον M ερωτήσεις"

Παραγωγή:
  για i = 0 .. N-1:
    pool = question_ids   (αντίγραφο)
    shuffle(pool)         (PHP shuffle() με seed βασισμένο στο i)
    group[i] = πρώτες M από το pool

    αν shuffle_options:
      για κάθε ερώτηση: shuffle(answer_ids)
```

**Γιατί αυτός ο αλγόριθμος:**
- Κάθε group τυχαιοποιείται ανεξάρτητα — ακόμα κι αν N×M > total (overlap), η **σειρά** εμφάνισης είναι πάντα διαφορετική.
- Αν N×M ≤ total (π.χ. 20 ερωτήσεις, 2 groups × 10), κάθε group παίρνει **εντελώς διαφορετικές** ερωτήσεις: shuffle μια φορά, split στα δύο.
- Αν N×M > total, τα groups μοιράζονται ερωτήσεις αλλά σε **διαφορετική σειρά** — αυτό αρκεί ως αντι-αντιγραφή όταν δεν έχουμε αρκετές ερωτήσεις.

**Σπόρος τυχαιοποίησης:** `mt_srand(crc32($exam_title . date('Ymd') . $i))` — ντετερμινιστικό αν ο καθηγητής ξαναπαράγει την ίδια μέρα, διαφορετικό άλλη μέρα.

---

## 4. Δομή του PDF

Ένα single multi-page PDF. Κάθε ομάδα ξεκινά από νέα σελίδα με mPDF `<pagebreak>`.

### Header κάθε ομάδας

```
┌─────────────────────────────────────────────────────┐
│  [Λογότυπο ιδρύματος]   ΔΙΑΓΩΝΙΣΜΑ Β' ΤΕΤΡΑΜΗΝΟΥ   │
│  Μάθημα: Μαθηματικά Β' Λυκείου                      │
│  Ημερομηνία: 15/05/2025   Διάρκεια: 45 λεπτά        │
│                           ┌──────────────────────┐   │
│  ΟΜΑΔΑ  Α                 │ Ονοματεπώνυμο: ______ │   │
│                           │ Τμήμα: _____________ │   │
│                           └──────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

### Σώμα ερωτήσεων

Reuse της `question_html()` από `export.php` — αποδίδει τις επιλογές χωρίς να δείχνει ποια είναι σωστή.

> **Γιατί δεν δείχνουμε τη σωστή:** Το `question_html()` εμφανίζει τις σωστές απαντήσεις (marked). Θα φτιάξουμε `question_html_exam()` παράλληλη function που **δεν** επισημαίνει τη σωστή — ο φοιτητής βλέπει μόνο τις επιλογές.

### Answer Key (προαιρετικό, τελευταία σελίδα)

Πίνακας με τις σωστές απαντήσεις ανά ομάδα και ερώτηση. Μόνο αν είναι τσεκαρισμένο το checkbox.

```
ΑΠΑΝΤΗΤΗΡΙΟ — ΟΜΑΔΑ Α
1. β   2. α   3. γ   4. α   5. δ   6. β   7. γ   8. α   9. δ   10. β
```

---

## 5. Σύνδεση με το υπάρχον UI

### Action button στο `question_pool.php`

Θα προστεθεί νέο κουμπί στο `action_bar`:

```php
array(
    'title' => $langCreateExamGroups,  // "Δημιουργία Εξεταστικών Ομάδων PDF"
    'url'   => "pdf_exam_groups.php?course=$course_code",
    'icon'  => 'fa-file-pdf',
    'level' => 'primary-label'
)
```

**Γιατί εκεί:** Ο καθηγητής βρίσκεται ήδη στο question pool, βλέπει τις ερωτήσεις, το context είναι σωστό. Είναι ο πιο φυσικός τόπος.

### Προαιρετικό: Action button στο `index.php` (λίστα ασκήσεων)

Ένα κουμπί «Export σε Ομάδες» δίπλα στο υπάρχον «Export» για κάθε exercise — οδηγεί στη νέα σελίδα με pre-selected πηγή το συγκεκριμένο exercise.

```
?course=...&exerciseId=5  →  pdf_exam_groups.php?course=...&fromExercise=5
```

**Γιατί:** Ο πιο συνηθισμένος use case: ο καθηγητής πηγαίνει στη λίστα ασκήσεων, βλέπει το «Διαγώνισμα», πατά «Export ομάδες» και προχωράει στη φόρμα με τις ερωτήσεις του exercise προ-επιλεγμένες.

---

## 6. Νέα language strings

Θα προστεθούν στα αρχεία γλώσσας (`modules/lang/el/common.inc.php` και `modules/lang/en/common.inc.php`):

```php
$langCreateExamGroups   = 'Δημιουργία Εξεταστικών Ομάδων PDF';
$langExamTitle          = 'Τίτλος Εξέτασης';
$langExamDate           = 'Ημερομηνία';
$langExamDuration       = 'Διάρκεια (λεπτά)';
$langNumGroups          = 'Αριθμός Ομάδων';
$langQuestionsPerGroup  = 'Ερωτήσεις ανά Ομάδα';
$langGroupLabel         = 'ΟΜΑΔΑ';
$langShowAnswerKey      = 'Εμφάνιση Απαντητηρίου';
$langShuffleOptions     = 'Ανακάτεμα Επιλογών';
$langGenerateExamPDF    = 'Δημιουργία PDF';
$langNotEnoughQuestions = 'Δεν υπάρχουν αρκετές επιλεγμένες ερωτήσεις. Χρειάζονται τουλάχιστον %d.';
$langAnswerKey          = 'Απαντητήριο';
```

---

## 7. Σύνοψη αρχείων που αλλάζουν / δημιουργούνται

| Αρχείο | Τύπος | Τι κάνει |
|---|---|---|
| `modules/exercise/pdf_exam_groups.php` | **ΝΕΟ** | Φόρμα + PDF generator |
| `modules/exercise/question_pool.php` | **ΤΡΟΠΟΠΟΙΗΣΗ** | +1 κουμπί στο action_bar |
| `modules/exercise/index.php` | **ΤΡΟΠΟΠΟΙΗΣΗ** | +1 κουμπί per-exercise στη λίστα |
| `modules/lang/el/common.inc.php` | **ΤΡΟΠΟΠΟΙΗΣΗ** | Νέα lang strings |
| `modules/lang/en/common.inc.php` | **ΤΡΟΠΟΠΟΙΗΣΗ** | Νέα lang strings |

**Καμία αλλαγή σε βάση δεδομένων.**

---

## 8. Ανοιχτά ερωτήματα (χρειάζεται επιβεβαίωση)

1. **Overlap ερωτήσεων:** Αν ο καθηγητής βάλει 20 ερωτήσεις, 3 ομάδες × 10, δεν χωράνε «διαφορετικές» παντού (20 < 30). Στρατηγική: κάθε group τυχαιοποιείται ανεξάρτητα (μπορεί να επαναλαμβάνονται αλλά σε διαφορετική σειρά). Είναι ΟΚ ή θέλεις σφάλμα/προειδοποίηση αν N×M > total;

2. **Output PDF:** Ένα ενιαίο PDF με page breaks (εύκολο εκτύπωση: ένα αρχείο) ή ξεχωριστό PDF ανά ομάδα (ZIP download); Πρόταση: ένα PDF.

3. **Shuffle επιλογών:** Θέλεις ο shuffle των απαντήσεων (π.χ. α/β/γ/δ να αλλάζει σειρά) να είναι **default on** ή **off** (checkbox);

4. **Λογότυπο στο header:** Να βάλει λογότυπο ιδρύματος στο header του PDF ή απλό κείμενο;

5. **Σειρά αρίθμησης:** Στο PDF να φαίνεται ο αύξων αριθμός ερώτησης (1, 2, 3…) ή να φαίνεται και ο εσωτερικός ID της ερώτησης στη ΒΔ;
